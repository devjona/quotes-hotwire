{
  "name": "app",
  "private": "true",
  "dependencies": {
    "@hotwired/stimulus": "^3.2.1",
    "@hotwired/turbo-rails": "^7.2.5",
    "esbuild": "^0.17.5",
    "sass": "^1.58.0"
  },
  "scripts": {
    "build": "esbuild app/javascript/application.js --bundle --minify --sourcemap --outfile=public/packs/app.js",
    "build:css": "sass app/assets/stylesheets/application.sass.scss public/packs/app.css"
  }
  
}
